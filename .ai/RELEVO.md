# RELEVO

- **Último turno:** claude-code
- **Fecha:** 2026-09-17
- **Rama:** mejora/calidad-y-web
- **HEAD:** (cerrar)

## Puente — no cambiar

NAS Synology `192.168.9.32` / `nas-nexara` anuncia `192.168.9.0/24`.

## Este turno — Evidencias del equipo y revisión jerárquica (aprobar / devolver / calificar)

Pedido de Adam (14-09): los encargados ven fotos, formularios y PDF embebidos de su cadena aunque la actividad esté cerrada (Christian todo; Luis ve a Antonio y al ingeniero; Antonio a su ingeniero); quien la creó también ve, sin editar. Cualquier superior de la cadena puede devolver pasos o todo, con observaciones y calificación de eficiencia; la actividad solo queda 100 % finalizada cuando se aprueba.

### Hecho

1. **API `GET /me/activities/:id/evidencias`** (`MyActivitiesService.teamEvidence`): personas en orden de la cadena (el responsable entra aunque no tenga fila de equipo), con su evidencia completa (fotos + GPS por foto, PDF, formulario, entrada/salida), estado de revisión, pasos por corregir, calificación e historial de revisiones. Alcance:
   - Todo: Christian, developer, `console.admin`/`evidences.review`, responsable y quien la creó (creador = solo lectura).
   - Encargado (LEAD activo): él y quienes la recibieron después (o a la vez, si no son encargados).
   - Jefe por organigrama (`managerId` hacia arriba) de alguien de la cadena: lo ve y lo revisa.
   - Los demás: solo lo suyo.
2. **API `POST /me/activities/:id/evidencias/:userId/revision`** `{decision: aprobar|devolver, pasos?, todo?, observaciones (≥5), calificacion 1–5}`. Puede revisar: todo/responsable/jefe por organigrama/encargado por encima. Nadie se revisa a sí mismo; quien solo reparte no tiene evidencia. Sin `ACTIVITIES_MANAGE` ni `EVIDENCES_REVIEW` (Antonio sí puede).
3. **Cierre por aprobación** (`ActivityEvidenceService`): cuando todos envían, la actividad pasa a **Por Validar** (antes quedaba Finalizada sola). `finalizeIfAllApproved`: Finalizada + `fechaFinalizacion` solo cuando la evidencia de cada ejecutor está aprobada; guarda `Activity.eficienciaScore` = promedio (1–5 → 0–100). Devolver (pasos o todo) deja la actividad **En Proceso** (ya no «Rechazada», que contaba como cerrada) y borra `fechaFinalizacion`. Devolver todo vacía la evidencia; los pasos devueltos respetan el tipo (tarea no pide PDF). Se puede devolver aunque ya esté aprobada.
4. **Registro de revisiones**: tabla `activity_evidence_reviews` (decisión, pasos, observaciones, calificación, copia de lo revisado) + `activity_evidences.eficienciaScore`. Migración `20260914160000_evidence_reviews`. Los endpoints viejos `approve`/`reject` (RBAC, los usa la app móvil) también registran, sin calificación.
5. **Avisos**: «lista para revisión» ahora llega también a los encargados (LEAD) y al jefe directo de quien envió; URL `/erp/actividades/:id/evidencias`. Aprobada/devuelta dice pasos, calificación, observaciones y si la actividad quedó finalizada. «Actividad lista para revisión» sustituye a «Actividad terminada».
6. **Historial**: eventos «Aprobada por…», «Devuelta para corregir por…», «Devuelta completa por…» con estrellas; «Cumplida» usa la hora de la última evidencia enviada (la aprobación llega después).
7. **Web `components/ops/EquipoEvidencias.tsx`**: tarjeta por persona (reparte/ejecuta, recibió de quién y cuándo, a quién la pasó, avance por paso con hora), fotos en visor grande (anterior/siguiente, teclado, «Ver en mapa»), PDF embebido + «Abrir PDF», formulario con etiquetas por tipo; barra «Aprobar / Devolver» para quien puede revisar; «↩️ Devolver este paso» en cada sección; ventana de revisión (aprobar/devolver, pasos o toda la actividad, estrellas con texto, observaciones obligatorias); historial con «Ver lo que se devolvió». Pestaña Evidencias: flujo de captura (solo si la ejecutas) + equipo. Pestaña Detalle: resumen compacto (reemplaza `ActivityEvidenceReviewPanel`, que leía `activity.activityEvidence` inexistente y nunca mostraba nada).
8. Web evidencias: `canUpload` en Core exige ser del equipo o responsable (antes cualquier superior veía el flujo y la API le creaba una evidencia vacía); el responsable de un despacho sin fila cuenta como «reparte».
9. **Push que faltaba (commit aparte):** entrada/salida propias, entrada sugerida por ACS y aviso a admins (`AttendanceService.avisar`), hora de comida próxima/vencida y aviso a admins (`LunchBreaksCronService.pushAll`) y «Nuevo acceso detectado» (`AuthService`, resuelve `NotificationsService` con `ModuleRef` porque NotificationsModule importa AuthModule) se escribían con `prisma.notification.create/createMany`: quedaban en la campana sin push ni socket. Ahora pasan por `NotificationsService.createNotification` con `relatedUrl` `/erp/asistencias` o `/erp/my-profile`. En pruebas sin el servicio, asistencia cae al `create` de antes.

10. **Archivos que se borraban en Docker (reporte de Adam 12:24: fotos en negro y PDF vacío en AN-0001).** La API guarda en `/app/uploads` (raíz del proyecto en el contenedor) y el compose de desarrollo solo montaba `./apps/api/uploads`: cada `docker compose build api` + `up -d` borraba todo lo subido. Las fotos, el PDF y la salida de Alejandro en AN-0001 (13–14 sep) se perdieron en los rebuilds de hoy (11:28 y 12:01) y no se pueden recuperar; en BD siguen las rutas (la API responde 404). `docker-compose.yml` ahora monta `./uploads:/app/uploads` como `deploy/docker-compose.nexara.yml` (producción ya lo tenía). Para AN-0001 un superior debe «Devolver → Toda la actividad» para que Alejandro vuelva a subir.
11. **Visor en `EquipoEvidencias`:** fotos, avatar y visor grande descargan con la sesión y, si el archivo no existe, muestran «Esta foto ya no está en el servidor» (antes cuadro negro). El PDF se descarga con la sesión y se dibuja con `components/PDFViewer.tsx` (pdf.js, `/pdf.worker.min.js`): `<object>` nunca iba a verse porque la CSP trae `object-src 'none'`.

12. **Hora de comida con justificación y aprobación (pedido de Adam 12:38).** En `/erp/asistencias` → Comidas no se podía registrar. Ahora:
   - API `lunch-breaks`: `GET mi-dia` (qué sigue: salida/regreso/listo, si ya es a destiempo), `POST checkin` / `PUT checkout` aceptan `justificacion`; fuera de 15:00–16:00 (regreso después de 16:05) sin justificación ≥5 → 400; con ella queda `revisionEstado` PENDIENTE y avisa a sus jefes por organigrama + Christian (`notifyLunchLate`). La hora del teléfono solo se acepta a ±10 min de la del servidor. `GET equipo?fecha=` (Christian/plataforma: todos; jefes: su organigrama; incluye «sin registrar»). `PATCH :id/revision` aprobar/rechazar (rechazar exige motivo; nadie se aprueba a sí mismo) → avisa a quien comió. Christian (gerencia@) ya no puede registrar (403). Migración `20260914190000_lunch_break_review` (columnas en `lunch_breaks`). `appUrls.erpLunchBreaks` → `/erp/asistencias?tab=comidas`.
   - Web `components/asistencias/ComidasPanel.tsx`: tarjeta «Tu hora de comida» (Salir a comer / Ya regresé, cámara en vivo → vista previa → justificación si es a destiempo), lista del equipo con filtros (Por aprobar, A destiempo, En comida, Sin registrar), fotos, justificaciones y Aprobar/Rechazar. La página ya no llama `lunch-breaks/today` (exigía ATTENDANCE_MANAGE) y abre la pestaña con `?tab=comidas`.
   - Spec `lunch-ventana-mexico`: las entradas fuera de ventana llevan justificación; 4 pruebas nuevas (sin justificación rechaza, con ella queda PENDIENTE, a tiempo sin revisión, hora de teléfono no esquiva la regla).

13. **Pizarra con más detalle (pedido de Adam 14:13).** Alejandro salía «Atrasado» con AN-0001 ya entregada: `team-board.service` tomaba como actividad actual cualquiera de su lista, aunque ya hubiera enviado su evidencia; «Inactivo» solo medía si checó entrada.
   - Ahora cuenta como en curso solo lo propio sin terminar (quien reparte un despacho no cuenta; terminada = envió su evidencia o la actividad se cerró). Estados: `activo`, `atrasado` (+ `currentLateMinutes`), `libre` (hoy terminó y no tiene nada abierto: `idleSinceAt` + `lastFinished` con `lateMinutes` contra `fechaMaxima`), `sin_actividad` (hoy no tuvo nada). `inactivo` ya no se asigna. Además `enEsperaAprobacion` (entregó y nadie aprueba) y `enCorreccion` (evidencia devuelta).
   - El «hoy» del tablero usa `workDayBounds` (México): con `toLocaleDateString` en el contenedor UTC cambiaba a las 18:00.
   - Web `/erp/pizarra`: «Atrasado 1 h 20 min», «Sin actividad desde hace 2 h 05 min» + «Finalizó a las 10:49 con 1 h 20 min de atraso» (rojo) o «a tiempo» (verde), etiquetas chicas «⏳ En espera de aprobación» / «↩️ Corrigiendo evidencia»; «Sin actividades hoy»; filtro «Terminó»; «Inactivo» solo si una API vieja lo manda.
14. **Reinicio de Docker/sesión (≈14:10):** los rebuilds de API y web de las 13:05 se cortaron (exit 4); se rehicieron con comidas + pizarra. El trabajo a medias de los agentes móviles quedó rescatado por `relevo salvar` en `c64bc09a` (sin compilar ni revisar).

15. **Fotos con nombre en el historial por persona** (`/erp/pizarra/[userId]`, pedido 14:32): cada foto lleva «📍 Entrada», «📷 Evidencia N» o «🏁 Salida» y usa `FotoProtegida` (aviso si ya no existe); el PDF usa `VisorPdf` (pdf.js, 400 px) en vez de `<object>` bloqueado por la CSP. `FotoProtegida` y `VisorPdf` ahora se exportan desde `EquipoEvidencias.tsx`.
16. **Volver a revisar una corrección (pedido 14:34, 15-09):** la API ya dejaba aprobar/devolver otra vez cuando la persona reenvía todo lo devuelto (queda COMPLETED + PENDING); lo que no se veía era que era una corrección. AN-0001 en BD: Christian devolvió «Fotos en sitio» 14:30 y Alejandro corrigió 14:31 → hoy está «Por Validar» esperando revisión (la captura de pantalla era la vista de Alejandro, que no puede revisarse a sí mismo).
   - API: `teamEvidence` devuelve `correctionSubmittedAt`; el aviso tras corregir dice «🔁 Corrección lista para revisión … Revísalo de nuevo» (`notifyEvidenceReadyForReview(…, correccion)`).
   - Web `EquipoEvidencias`: estado «🔁 Corrección por revisar», barra «Corrigió lo que se le devolvió (Fotos en sitio) · fecha. Revisa la corrección y apruébala o devuélvela de nuevo», pasos corregidos en azul «🔁 Corregido · hora»; mientras corrige: «Cuando envíe la corrección podrás aprobarla o devolverla otra vez».

### Verificado

- `prisma generate` + `tsc --noEmit` API limpio. Web: sin errores nuevos (siguen los 4 previos: CommandPalette ×2, evidence-flow-helpers, module-guides).
- Docker: `build api` + `prisma migrate deploy` («All migrations have been successfully applied») + `up -d api`; «Nest application successfully started»; `activity_evidence_reviews` existe (0 filas). `GET …/evidencias` y `POST …/revision` → 401 sin sesión.
- Web: `build web` + `up -d`; `/erp/actividades/1/evidencias` 200; el bundle trae «Devolver este paso» y «Ver lo que se devolvió».
- Cadena real AN-0001 en BD: Luis (responsable, LEAD) → Antonio (LEAD) → Alejandro (TECNICO); jefe de Alejandro y Carolina = Antonio. Con las reglas: Antonio ve y revisa a Alejandro, no ve a Luis. AN-0001 quedó «Finalizada» por el cierre automático viejo con evidencia sin aprobar: no se migraron datos; la pestaña la muestra «Por revisar».
- Push: `tsc` API limpio; `jest src/attendance src/auth` 6 suites / 49 pruebas OK; `build api` + `up -d` arrancó sin errores de dependencias.
- Archivos (12:30): `up -d api` con `./uploads:/app/uploads` montado (se ven los 11 archivos viejos de `uploads/activities`). `tsc` web sin errores nuevos; `build web` + `up -d`; `/erp/actividades/1/evidencias` 200, `/pdf.worker.min.js` 200 (pdf.js 3.11.174 = `pdfjs-dist`); el bundle trae «Esta foto ya no está en el servidor» y «El PDF ya no está en el servidor».
- Comidas + pizarra (14:29): `tsc` API limpio, `jest src/me` 3 suites / 39 OK, `tsc` web sin errores nuevos. Docker: `build api` + `migrate deploy` (aplicó `20260914190000_lunch_break_review`; `lunch_breaks` tiene `checkinJustificacion`, `revisionEstado`, `revisadoPorId`) + `up -d api` («Nest application successfully started»); `lunch-breaks/mi-dia`, `lunch-breaks/equipo`, `me/board` → 401 sin sesión. `build web` + `up -d`; `/erp/pizarra` y `/erp/asistencias` 200; bundle con «Sin actividad desde hace», «En espera de aprobación», «Corrigiendo evidencia», «Salir a comer». Dato real AN-0001: fechaMaxima 14-09 02:00, Alejandro envió 10:49 (1249 min) → la tarjeta dirá «Finalizó a las 10:49 con 20 h 49 min de atraso» + «En espera de aprobación».
- Corrección + fotos con nombre (15-09 08:41): `tsc` API limpio y web sin errores nuevos; Docker Desktop se arrancó (estaba apagado tras reiniciar la máquina); `build api web` + `up -d`; «Nest application successfully started»; `/erp/actividades/1/evidencias` y `/erp/pizarra/15` 200; `me/activities/1/evidencias` 401 sin sesión; bundle con «por revisar», «lo que se le devolvió», «Corregido», «devolverla otra vez». Los agentes móviles se detuvieron por límite de uso; lo que llevaban quedó en `6741f41d` y se reanudaron.

17. **App Android al día con Core (agente, 15-09):** ERP solo (OPS dentro de ERP en navegación, permisos y enlaces), Actividades con pestañas Mis actividades / Mi equipo y tablero con los estados nuevos, Mis actividades (orden con justificación, auto-asignación, despacho, reprogramar), captura con CameraX en vivo + vista previa + GPS por foto + PDF + formulario + correcciones, revisión del equipo (estrellas, pasos o todo, historial, «Corrección por revisar»), fotos con nombre y aviso de archivo faltante, pestañas Detalle / Evidencias / Historial, Comidas en Asistencias y enlaces de avisos `/erp/…` y `/ops/…`. Archivos nuevos en `ui/console/activities`, `ui/console/comidas`, `data/api/CoreActivitiesApi.kt`, `ComidasApi.kt`, `ui/common/LiveCameraCapture.kt`, `ProtectedAssets.kt`. Verificado: `:app:compileDebugKotlin` OK y `:app:testDebugUnitTest` 490 pruebas, 0 fallos; `build.gradle.kts`, `gradle.properties`, `google-services.json` y Manifest sin cambios. **Sin probar en dispositivo** (cámara, GPS, push, API real). Pendiente: Comidas solo muestra hoy; «Asignar actividad» no preselecciona a la persona; la pantalla vieja de aprobar/rechazar evidencias sigue en el código.

18. **App iOS al día con Core (agente, 15-09), SIN COMPILAR:** revisión del equipo (`UI/Core/TeamEvidenceView.swift`, antes `ActivityCoreDetailView` llamaba vistas que no existían y el target no podía compilar), Comidas (`Data/LunchCoreRepository.swift`, `UI/Core/ComidasView.swift`), tablero con los estados nuevos, enlaces de avisos `LunchBreak` / comidas, foto de asistencia con cámara en vivo + GPS, captura de evidencias (`EvidenceCaptureFlowView.swift`), Firebase Messaging por SPM (solo arranca si existe `GoogleService-Info.plist`; token FCM a `devices/push-token`). Revisión hecha aquí: sin tipos duplicados de nivel superior (los repetidos son anidados), sin plist ni llaves agregadas, `project.yml` con FirebaseMessaging. Riesgos: la primera compilación en Xcode puede dar errores de tipos o concurrencia; `MyLunchBreaksView`/`LunchBreaksAdminView` quedan sin uso; la cámara de comida pide GPS aunque no lo manda.

19. **Servidor de prueba desplegado + textos de las apps (pedido de Adam 16-09 16:10: «Cannot GET /api/me/board», logo del login ilegible, versión con «(7)», presentación que dice CRM).** Despliegue: el primer `update.sh --with-migrate` NO reconstruyó nada porque hice `git pull` antes y el script no vio commit nuevo (y migró con la imagen vieja: «No pending migrations»). Hay que usar `./deploy/update.sh --force-all --with-migrate` si el pull ya se hizo. Con eso: `nexara-api` y `nexara-web` recreados 22:26 UTC, 8 migraciones aplicadas (hasta `20260914190000_lunch_break_review`), `/api/me/board` 401 sin sesión (antes 404), `lunch-breaks/equipo` 401. El build web del servidor falló el chequeo de tipos (`CommandPalette.tsx` entityActions) y reintentó ignorándolos; se corrigió aquí junto con `evidence-flow-helpers.ts` y la guía faltante `erp-clients` en `module-guides.ts` → `tsc --noEmit` web limpio. Apps: logo del login y del bloqueo sobre verde fuerte `#0F766E` (el PNG lleva «NEXARA» en blanco), iOS usa ahora el mismo logo (`Assets.xcassets/LogoNexara.imageset`); versión solo `v1.0.1` (sin código ni «debug»); presentación Core (Tus actividades / Asistencia y comida / Equipo conectado); filtros de avisos Core (Actividades / Asistencia / Chat / Otras) en web, Android e iOS; Mi perfil sin «Integra/ACS» para el empleado (web, Android, iOS); canales push Android renombrados (Servicios, Actividades, Revisiones); cola offline sin viático/vehículo/ticket/cotización; «Tipo de servicio»; Info.plist sin «comprobantes». Verificado: gradle compile+tests+manifest OK, `ios-static-check` limpio, APK contra `https://api.nexara.com.mx/api` instalada en el emulador, login con logo legible y «NEXARA · v1.0.1». La presentación sale después del primer inicio de sesión (no se pudo ver sin credenciales). Además se borró código Android sin uso que aún decía viáticos/cotización/«Cambiar panel»: seis pestañas de `ActivityDetailTabs.kt` (solo queda `ActivityInfoTab`) y `NxModuleScaffold`; compila y pasan las pruebas.

20. **Avisos push tipo WhatsApp y sesión que no se cierra (pedido de Adam 16-09 16:52).**
   - **Causa raíz de «no llegan push»:** `apps/mobile-native/android/app/google-services.json` es un PLACEHOLDER (`nexara-placeholder`, clave falsa → `FIS_AUTH_ERROR`, el teléfono nunca obtiene token) y en el servidor `FIREBASE_SERVICE_ACCOUNT_JSON` está vacío; `UserPushEndpoint` tiene 0 filas. Nunca hubo push móvil. **Falta que Adam cree el proyecto Firebase** (Android `mx.nexara.mobile.nativeapp` + iOS con llave APNs), baje `google-services.json`/`GoogleService-Info.plist` y ponga la cuenta de servicio en `deploy/.env.nexara`.
   - **Mientras tanto (sin Firebase):** cada aviso viaja también por el socket (`push:show`, mismos datos que FCM) y Android lo pinta como notificación del sistema mientras el proceso vive (app abierta, en segundo plano o con la jornada/GPS activa). Si FCM también lo entrega, se descarta el repetido (`message_id` / `nexara_notification_id`).
   - **API:** FCM a Android solo-`data` con prioridad alta (antes llevaba `notification` y caía en un canal sin emergente); iOS con `alert`+sonido+`thread-id`; tokens muertos se borran. Nuevos datos: `kind` (chat/event), remitente (nombre/foto), `thread_id`, `thread_title`, `message_id`, `sent_at`; canal `attendance`. Avisos nuevos o reescritos (a jefes por organigrama + Christian + admins del depto): «🟢 X entró a trabajar» / «🟠 … con retardo» (plantilla 9:00/8:00 +15 min) / «🔴 terminó su jornada» con hora; comida sale/regresa con hora; «▶️ X inició AN-…» (foto de entrada), «📷 subió N fotos», «📄 hoja de servicio», «📝 formulario» (responsable, creador, encargados, jefes; un aviso por visita que se actualiza); reasignación (nuevo, anterior, creador, Christian); asignación con título, cliente y fecha; **cada mensaje de chat** (directos y grupos, sin silenciados, sin duplicar a mencionados). Arreglos: dedupe ya no se come el aviso de otra persona; alerta SLA filtraba por `isSuperAdmin` (no existe) y nunca salía; se quitó el aviso duplicado de entrada/salida a todos los admins sin filtro de empresa.
   - **Sesión:** `POST auth/session/refresh` acepta el JWT vencido (firma válida) y lo renueva mientras la sesión no esté revocada, el usuario activo y con uso en 30 días. Android (`data/session/SessionRefresher.kt`, `SessionRefreshPolicy.kt` + 15 pruebas) e iOS (`ApiClient`/`AuthRepository`): 401 → renovar una sola vez para todas las peticiones → reintentar; solo cierra sesión si la renovación da 401. Renovación proactiva al abrir/volver y cada 30 min; socket arranca con la sesión guardada; iOS guarda la sesión en llavero accesible tras el primer desbloqueo (antes un relanzamiento en segundo plano la «perdía»).
   - **Android avisos:** canales nuevos `nx2_chat/ops/approvals/attendance/alerts/default` (alta importancia; se borran los `nexara_*` viejos), ícono monocromo `ic_stat_nexara`, chats con `MessagingStyle` apilados por conversación (últimos 8, avatar o iniciales, grupos con nombre, sección «Conversaciones»), eventos con etiqueta y avatar que se actualizan en su lugar, sin aviso si ese chat está abierto, limpieza al cerrar sesión. `src/debug/.../DebugPushReceiver.kt` (solo debug) simula avisos: `adb shell am broadcast -a mx.nexara.DEBUG_PUSH -n mx.nexara.mobile.nativeapp/.push.DebugPushReceiver --es kind chat --es title … --es body … --es thread_id chat-7`.
   - **Verificado:** `tsc` API limpio; jest 174/175 (el que falla, `activities.controller.spec` «scopes an ops manager», ya fallaba: mock sin `user.findMany`); Android compile + 199 pruebas + manifiestos debug/release; `ios-static-check` limpio (iOS sin compilar). En el emulador: aviso emergente de asistencia y panel con chats apilados («Luis Hernández» 2 mensajes, grupo «#obras»), actividad y asistencia con avatar (capturas nx30/nx31). No probado con sesión real ni FCM real (falta Firebase).

21. **Firebase real configurado (con Adam en Chrome, 16-09 17:34).** El proyecto `nexara-app` ya existía (FCM v1 habilitado) pero con la app Android vieja `mx.nexara.mobile`. Se registraron **NEXARA Android** `mx.nexara.mobile.nativeapp` (huellas SHA-1 debug `92:B0:F2…` y release `A9:86:2A…`) y **NEXARA iOS** `mx.nexara.mobile.NexaraApp`. `android/app/google-services.json` reemplaza el placeholder (el emulador ya obtiene token FCM real); `ios/Resources/GoogleService-Info.plist` ahora se versiona (el workflow de TestFlight nunca lo escribía pese al comentario). La API acepta `FIREBASE_SERVICE_ACCOUNT_JSON` en JSON o base64. **Clave de servidor instalada (17:47):** Adam la generó y se instaló con `scripts/firebase/instalar-clave-servidor.ps1 -BorrarDescarga` (respaldo de `.env.nexara`, base64, API healthy con proyecto `nexara-app`, copia local borrada). **Probado con FCM real:** 3 avisos (asistencia, chat, actividad) y luego, con el proceso de la app muerto (`kill -9`), un chat que despertó la app y salió como aviso emergente apilado con el anterior. **Pendiente de Adam:** llave APNs `.p8` (Apple Developer → Keys; pide iniciar sesión) subida en Firebase → Cloud Messaging → app iOS → «Subir» (desarrollo y producción) con Key ID y Team ID; sin eso iOS no recibe push.

22. **Azul, íconos vectoriales, textos de avisos, nomenclatura oficial y RH (pedido de Adam 16-09 17:52; cortado por límite de uso a las 18:09 y retomado 20:25, rescate `15182c85`).**
   - **Avisos (API):** sin emojis, con el nombre de la actividad en vez de AN-0001, nombre corto de la persona (`nombreCorto`), horas «8:02 a. m.» y dato `icon` (27 claves: entrada, entrada_tarde, salida, comida_*, actividad_nueva/inicio, fotos, documento, formulario, reasignada, reprogramada, despacho, por_revisar, correccion, aprobada, devuelta, finalizada, atraso, vencida, chat, mencion, seguridad, aviso) en `notification-push-meta.ts`.
   - **Apps y web en azul** (#2563EB / #1E40AF / #DBEAFE) y **sin emojis** (quedan solo las reacciones del chat): Android con `NxGlyph`/`NxIcons` y `ic_nx_*`; aviso con foto o iniciales + insignia de color del evento (`push/NotificationEventIcons.kt`); iOS con `NxBrand.swift` y SF Symbols (`NotificationIcon.swift`); web con `@mui/icons-material` y tokens en `globals.scss`.
   - **Número de empleado oficial = nomenclatura de RH** (`src/rrhh/nomenclatura.ts` + 8 pruebas; `GET /api/hr/nomenclaturas/auditoria`). Migración `20260917030000_rrhh_nomenclatura` (perfil: correoContacto, imssAlta, documentosPendientes, sueldoSemanal, funciones, nomenclaturaOrigen; enum `SLA_BREACH`). `scripts/importar-rrhh.js` aplicado en el servidor de prueba: 10 actualizados, 4 altas **inactivas sin contraseña** (José Antonio Ramírez Salazar `jose.ramirez@`, Daniela Hernández Cruz `daniela.hernandez@`, Juan José González Rojas `juan.gonzalez@` con clave generada JJ00092606, Roberto Paul Vivanco López `roberto.vivanco@`), Paulina pendiente por indicación de Adam; bitácora `audit_logs` RRHH_IMPORT/RRHH_ALTA. Los datos de RH (RFC/CURP/NSS/sueldos) NO están en el repo. Claves cargadas tal cual; la auditoría marca inválidas LJ75100126, IC91052710, CJ26210822, DM91030125, IR93050000, JS03052901 y diferentes DH95052606, JA91091706 (sugiere la correcta según CURP e ingreso).
   - **Roberto solo servicios:** equipo de Antonio en API (`DISPATCH_POOLS`, pizarra), web, Android e iOS (recibe solo `servicio`; entra al pool de servicios, no al de proyectos).
   - **Fotos de las personas:** importación desde el ACS (`integra/acs-avatar-*.ts`, `scripts/import-acs-avatars.js --acs-company=2`) empareja 11 de 12 empleados, pero el DS-K1T devuelve 404 al descargar el rostro (FDSearch con `FDID/FPID` sí lista la `faceURL`, la descarga falla con y sin `@WEB…`). Fuente real: la foto de la checada se vuelve foto de perfil si la persona no tiene una (`attendance.service.ts`); las fotos viejas estaban como data-URI y no se reutilizan.
   - Verificado: tsc API/web limpios, jest API áreas tocadas, Android compile + 190 pruebas + manifiesto, `ios-static-check` limpio, capturas del emulador (login azul, avisos con insignias). Desplegado en el servidor de prueba (`1c52b8c3`).

23. **Empleados completos, cuentas no empleadas, permisos, Recordarme, reacciones y avisos de inicio de sesión (pedidos de Adam 16-09 21:00–22:40).**
   - **Plantilla completa en el servidor de prueba:** los 15 empleados declarados activos con su nomenclatura (José Antonio, Daniela Hernández, Juan José, Roberto y Paulina `finanzas@` dados de alta). **Los 5 nuevos no tienen contraseña utilizable**: un administrador debe asignarla.
   - **Cuentas que no son empleados** (`common/platform-accounts.ts` y espejo en web `lib/platform-accounts.ts`, Android `access/PlatformAccounts.kt`, iOS `CoreModels.swift`): `gerencia@`, `developer@`, `claudia.bernal@` (tester, mismos permisos que Christian: superadmin por correo) y `play.review@` no salen en pizarra, asignables, asistencias, RH ni auditoría de nomenclaturas (`9a28020e`, `4cac03f4`).
   - **403 corregidos** con un verificador de RBAC dentro del contenedor por cada perfil: `CORE_OLA1_URL_RULES` (asistencia propia, comidas, `integra/identity/me`, actividades, evidencias, proyectos) y permisos base para oficina (`V2_CORE_OFFICE_ROLES`) (`dad8869a`, `06593d5f`). Quedan negados a propósito: endpoints de líderes (`gps/team` para coordinadores; la app atrapa el 403) y 14 en la cuenta demo de tiendas (rol ceo sin superadmin).
   - **«Recordarme»** en Android (`data/RememberMe.kt`) e iOS (`Session/RememberMe.swift`), activo por defecto; sin marcar, la sesión termina al cerrar la app (`40d33f6a`).
   - **Reacciones del chat con quién y en qué orden** (API `users[]` por `createdAt` + prueba; web tooltip/modal; Android e iOS hoja de quién reaccionó) (`4cac03f4`).
   - **Aviso de inicio de sesión estilo Instagram** (`cde7e2b5`): «Nuevo inicio de sesión en Galaxy S24 Ultra» / «Iniciaste sesión desde iPhone 16 Pro Max (iOS 18.1) · app NEXARA el …». `device-detector.ts` arma `friendly` (4 pruebas); Android manda `X-Device-Name`/`X-Device-OS`; iOS `Data/DeviceIdentity.swift` traduce el identificador a nombre comercial. Los avisos viejos conservan su texto anterior.
   - **Pulido tras recorrer el emulador (este commit):** preview del chat legible en API/web/Android/iOS (`[@Nombre](user:2)` → `@Nombre`, enlaces a su etiqueta, sin emojis); tema Android sin los contenedores lila de Material 3 (barra inferior y tarjetas en neutros azulados); chips de Clientes con mayúscula; la cola offline ya no encola `/auth/*` ni `devices/push-token` y descarta pendientes de otro servidor (una APK compilada contra `10.0.2.2` dejaba «Pendientes de sync: 1» para siempre); el aviso de sync/sin conexión ya no se encima al reloj (`statusBarsPadding` + `consumeWindowInsets`).
   - **Ojo al compilar la APK para el servidor:** la propiedad es `-PDEV_API_BASE_URL=https://api.nexara.com.mx/api` (sin ella, debug apunta a `10.0.2.2:3001`). Instalar siempre con `adb install -r`; **nunca `pm clear` ni desinstalar** (borra la sesión de Adam).
   - Verificado: tsc API/web, jest chat (7), Android compile + pruebas, `ios-static-check`; emulador con sesión de Christian recorriendo Actividades (15 empleados, sin cuentas no empleadas), Asistencias, Chat, Clientes, Mi perfil y Avisos sin 4xx/5xx en `docker logs nexara-api`. Probar cada perfil requiere que Adam inicie sesión (el agente no escribe contraseñas).

24. **Versión 1.0.2 lista para Google Play (pedido de Adam 16-09 23:15: «modifica todo para subir la nueva versión, quítale el 7-8, pon nuevas capturas»).**
   - **AAB firmado:** `VERSION_CODE=8`, `VERSION_NAME=1.0.2` con `scripts/build-play-aab.ps1 -VersionCode 8 -VersionName 1.0.2` (23 MB). Copia en `apps/mobile-native/play-releases/NEXARA-1.0.2-v8.aab` y mapping `mapping-v8-1.0.2.txt` (carpeta ignorada por git). **Adam sube el bundle**: en Play Console ya existe el borrador de Producción con nombre de versión **«1.0.2»** (antes «7 (1.0.1)») y notas de la versión en es-419; al subir, revisar que el nombre siga «1.0.2».
   - **Ficha de Play Store guardada (sin enviar a revisión):** 8 capturas viejas (verdes, CRM, cotizador, tickets, «No se pudo cargar») reemplazadas por 7 nuevas con marco azul (`play-assets/screenshots/phone/nexara-01…07.png`); descripción corta y completa reescritas para Core (sin CRM/OPS/STUDIO/LAB/cotizador/biometría, que ya no existen y podían causar rechazo por metadatos engañosos). Se envía a revisión junto con la versión desde «Resumen de publicación».
   - **Capturas con datos ficticios:** `StoreDemoInterceptor` (solo `src/debug`, se instala con `StoreDemoProvider` vía `ApiDebugHooks`) contesta los GET con `assets/store-demo/*.json` cuando existe `files/store_demo/serve`; `scripts/store-demo/generar-fixtures.py` inventa equipo, asistencias, chat, clientes y avisos, y `enmarcar-capturas.py` arma los PNG 1080×1920. Procedimiento en `play-assets/ASSETS-README.md`. El emulador quedó restaurado (sin bandera, zona horaria GMT, barra de estado normal) y con la sesión de Adam.
   - **Fuga corregida en la API:** `GET users/profile/me` y `users/:id/profile` devolvían el `User` completo con `passwordHash` y `mfaSecret`. Nuevo `common/security/strip-secrets.interceptor.ts` global (APP_INTERCEPTOR) los quita de toda respuesta sin mutar el objeto original (+3 pruebas).
   - **Envío (17-09 00:05–00:40, Adam pidió «aprieta todos los botones»):** Play frenó la versión con dos errores: permiso sensible sin declarar (`ACCESS_BACKGROUND_LOCATION`, nuevo desde v7) y declaración de servicio en primer plano de ubicación sin hacer. Se **quitó `ACCESS_BACKGROUND_LOCATION`** del manifiesto y el botón «Permitir todo el tiempo» de Asistencias: el servicio `JornadaGpsService` (tipo `location`) arranca desde la checada con la app abierta y sigue con la pantalla apagada; si Android mata el proceso, el rastreo vuelve al abrir Asistencias (`reanudarGpsSiJornadaAbierta`). Nuevo AAB **versionCode 9** (1.0.2) en `play-releases/NEXARA-1.0.2-v9.aab`; el 8 se retiró del borrador (queda en la biblioteca, ya no se puede reutilizar). **Declaración FGS guardada** con «Compartir ubicación (acción iniciada por el usuario)» y video en Drive de Adam (`NEXARA-GPS-de-jornada.mp4`, enlace público de solo lectura). El video se grabó con el modo `checador` del `StoreDemoInterceptor` (checada y puntos GPS simulados, nada se escribe en el servidor; verificado en `Attendance`) y `ApiDebugHooks.forceSelfCheckIn`, ubicación simulada con `cmd location providers set-test-provider-location`. **Falta:** que Adam arrastre el AAB v9 al borrador (pesa 23 MB y la extensión de Chrome solo sube 10 MB), luego Siguiente → Guardar → Resumen de publicación → Enviar a revisión.
   - **Revisado en Play Console:** un fallo en v7 (08-09, 1 usuario): `HttpException 403` sin atrapar dentro de una corrutina de Retrofit; coincide con los 403 de RBAC corregidos en `dad8869a`/`06593d5f`, pero queda pendiente auditar `launch {}` sin `try` en Android. Recomendaciones no bloqueantes: R8 con tasa de optimización 45 % (reglas `-keep` amplias por paquete; ajustarlas exige probar el AAB en un teléfono real) y APIs obsoletas de edge-to-edge que vienen de Material `datepicker` y `security-crypto`.

25. **Geocerca de 100 m en actividades + cobro de Google Cloud + usuarios (pedidos de Adam 17-09 08:43–09:00).**
   - **API** (`0ede141c`): tabla `activity_geofence_alerts` + tipo `ACTIVITY_OUT_OF_ZONE` (migración `20260917140000_activity_geofence`). `ActivityGeofenceService`: cada punto de `POST gps` de quien tiene actividad iniciada (foto de entrada con GPS, estatus no cerrado) se mide contra su entrada; al pasar de 100 m abre alerta (una por salida, guarda la distancia máxima) y avisa por push a la persona, sus jefes por organigrama, Christian, responsable, creador y encargados (`notifyActivityOutOfZone`); al volver la cierra (`returnedAt`). La foto de salida (y su corrección) se rechaza con 400 a más de 100 m. `GET activity-evidence/:id/geocerca` (inicio, recorrido, alertas) y `POST .../geocerca/alertas/:alertId/justificacion` {motivo ≥5, fotoBase64} → avisa a jefes. `me/activities/:id/evidencias` trae `alertasZona` por persona. Pruebas: `geocerca.spec`, `activity-geofence.service.spec`.
   - **Android** (`11b51477`): tarjeta «Ubicación de la actividad» en captura de evidencias (dentro/fuera, última ubicación, recorrido, alertas con «Justificar con motivo y foto»), bloqueo de la foto de salida a >100 m, `JornadaGps.iniciarActividad/terminarActividad` (puntos cada ~2 min con `actividadId`, alta precisión; se reanuda tras reinicio), «Salidas de zona» en evidencias del equipo, ícono `fuera_zona`. **Web** (`e33a0fec`, agente) e **iOS** (`7819f5b5`, agente, sin compilar) con la misma función.
   - **AAB versionCode 10 (1.0.2)** con la geocerca: `play-releases/NEXARA-1.0.2-v10.aab`. El 9 nunca se subió.
   - **Cobro de ~2,000 MXN en Google Cloud:** no es Firebase. Es la cuenta de facturación «Mi cuenta de facturación de Maps» `012F2D-CDBF7A-362D58` (cerrada): **Places API (New) · Text Search Enterprise**, proyecto «My First Project» (`project-3f10b190-db54-438b-bef…`), 1–17 sep, MXN 2,185.56 + IVA = 2,535.25. Google cobró 2,000 el 17-09 a la Visa ••1033 (cargo por umbral) y quedan 535.25 pendientes. La clave «Maps Platform API Key» (creada 11-12-2025) no tiene restricciones. El código que llama a Places no está en NEXARA-app, CONDUIT, VOLTIA, ZYNORATEK, PRODUCTOS ni `C:\dev\scripts` (no se buscó en carpetas protegidas). La otra cuenta `01A804-…` está cerrada con saldo 0 (el «2,000 disponibles» ahí es el límite de pago, no deuda).
   - **Usuarios:** José Antonio (id 39) ahora con puesto «Coordinador de Soporte» (rol sin cambios, `ing_soporte`). Hoja `Downloads/NEXARA-usuarios-v3.xlsx` generada por agente sin tocar contraseñas (v2 intacta; 23 filas; 6 agregadas). No se generaron ni escribieron contraseñas ni se inició sesión en las cuentas (prohibido manejar contraseñas).

26. **Más pedidos del 17-09 (09:15–09:40) y estado de la versión 1.0.2.**
   - **Mi perfil** (`015b8417` Android, `06c27d6c` web): permisos traducidos y agrupados por módulo («Lo que puedes hacer», `access/PermissionLabels.kt` + pruebas; lo heredado de paneles fuera de Core, plegado). Christian, Claudia y cuentas de sistema ya no ven «Perfil completo» ni se les piden CURP/RFC/NSS/INE/domicilio (web y Android).
   - **GPS más espaciado** (`61a4af61`, pedido de ahorro): jornada cada ~30 min o 500 m; con actividad cada ~10 min (alta precisión) para la geocerca. iOS igual. La ubicación del teléfono no cuesta en Google Cloud.
   - **Campana web** (`ef2199f9`): abrirla marca todo como leído (`PATCH notifications/read/all`), las nuevas siguen resaltadas en esa vista. En Android lo hace el agente de UX (abajo).
   - **Play Console:** ficha con 8 capturas (la 8 «Alertas si alguien sale de zona», con la notificación real de la app) y descripción nueva guardadas; declaración de servicio en primer plano guardada con video en Drive; borrador de Producción «1.0.2» con notas y **sin bundle**. Falta que Adam suba `play-releases/NEXARA-1.0.2-v10.aab` (23 MB; la extensión de Chrome sube hasta 10 MB) y luego Siguiente → Guardar → Resumen de publicación → Enviar a revisión.
   - **Perfiles de RH:** la tabla que volvió a pegar Adam ya está cargada en los 15 empleados (los huecos coinciden con la tabla: Israel y Mónica sin RFC/CURP/NSS, Luis Joel y Mónica sin teléfono, Daniela Hernández sin sueldo, Roberto sin correo). No se purgan.
   - **Agentes en curso (ramas aparte, sin integrar):** `ux/android-friccion` (fricción en todas las pantallas: quitar «Salir» del encabezado, títulos duplicados, chat con un solo «+», chips de filtros en una fila, una sola acción en Mi jornada, avisos marcados al abrir); `feat/permisos-cancelacion-faltas` (API + web: solo Christian elimina/desactiva clientes; cancelar o pasar actividad solo superiores con motivo y push; falta justificada solo por Christian); `demo/semana` (scripts `apps/api/scripts/demo-semana/`: purgar operación —clientes, actividades, asistencias, chat, avisos—, sembrar una semana realista y purgar lo sembrado). **Purga y siembra aún NO ejecutadas**: correr solo tras `pg_dump` completo.
   - **Cobro Google Cloud:** ver punto 25. Recomendación a Adam: dejar cerrada la cuenta de facturación de Maps o deshabilitar Places API (New) en «My First Project», restringir la clave «Maps Platform API Key» y poner alertas de presupuesto; `apps/web/app/components/Map.tsx` trae una clave de respaldo escrita en el código (no se tocó, veto de credenciales).

27. **17-09 (10:00–11:15): 1.0.2 en revisión, cumpleaños, permisos, proyectos y purga con semana demo.**
   - **Google Play:** «Enviar 4 cambios a revisión» pulsado; estado «Tus cambios están en proceso de revisión» (1.0.2, bundle 10). Lo que se integró después (UX Android, cumpleaños, permisos) va en la siguiente versión (código 11).
   - **UX Android** (merge `aa1ce53a`, rama `ux/android-friccion`): sin «Salir» en la barra (cerrar sesión en Mi perfil), bandeja que se da por vista al abrir, filtros en una fila, botón del siguiente paso, Mi jornada con un botón, chat con búsqueda y un «Nueva conversación», clientes con FAB. Probado en emulador: la campana baja de 39 a 0.
   - **Cumpleaños y aniversarios** (`fcef4122`, `apps/api/src/celebrations/`): tarea 8:00/11:00/14:00 (hora de México), una vez por día; aviso a quien celebra y a su empresa; nacimiento del perfil o del CURP; nunca la edad. `GET me/celebraciones/hoy` para el banner. **Ya funcionó en producción:** 17-09 11:00, 18 avisos por el cumpleaños de José Antonio (push confirmado en el emulador). Web con banner (merge `b4860c8b`).
   - **Permisos** (merge `15e39faa`, rama `feat/permisos-cancelacion-faltas`): clientes solo Christian desactiva/elimina; cancelar o pasar actividad solo superiores con motivo y push; faltas justificadas solo Christian. Migraciones `20260917180000`, `20260917190000`, `20260917200000` aplicadas. Falla previa y ajena: `activities.controller.spec.ts › scopes an ops manager…`.
   - **Proyectos** (`8ba94ed3`): `POST operational-projects/:id/desactivar|reactivar` (ON_HOLD = «Inactivo») y `DELETE` lógico, solo Christian; `OperationalProject` entra al borrado lógico de PrismaService.
   - **Purga y semana demo en producción** (merge `cf80056e`, `apps/api/scripts/demo-semana/`): probado primero completo en copia `nexara_prueba_demo` (ya borrada). En producción: purgados 351 registros (clientes, actividades, asistencia, GPS, chat, avisos); sembrado lote **`semana-2026-09-14`** (1973 filas, 283 archivos; `SIN_GPS_EN_CURSO=SI`, oficina 19.0740,-98.2780). Avisos de días anteriores marcados leídos; los 18 de cumpleaños de hoy se reinsertaron para que la tarea de las 14:00 no repita. Quitar la demo: `docker exec -i -e CONFIRMAR=SI -e LOTE=semana-2026-09-14 -w /app/apps/api nexara-api node - < apps/api/scripts/demo-semana/purgar-demo.js`.
   - **Respaldos en el servidor:** `/root/backups/nexara_db-20260917-1644-antes-permisos-celebraciones.dump` (completo, 1.6 GB) y `nexara_db-20260917-1708-antes-purga-sin-auditoria.dump` (6 MB, sin datos de `audit_logs`).
   - **Servidor:** se mató un bucle viejo `while pgrep -f deploy/update.sh` que se encontraba a sí mismo; para esperar despliegues usar `pgrep -f "[d]eploy/update\.sh"`.
   - **En curso (agentes, sin integrar):** Android e iOS: botones de Christian para clientes y proyectos, íconos y banner de cumpleaños, cancelar/pasar actividad, avance anterior y faltas justificadas.

### Falta probar a mano

1. Antonio → actividad de Alejandro con evidencia enviada → Evidencias: ve fotos/PDF/formulario, «Aprobar» con estrellas y observaciones → actividad Finalizada; Luis y Christian reciben aviso.
2. Luis → «Devolver este paso» en fotos → Alejandro ve «Corrigiendo: Fotos en sitio» y solo repite ese paso.
3. «Toda la actividad» → Alejandro rehace desde la foto de entrada; en el historial «Ver lo que se devolvió» muestra lo anterior.
4. Quien creó la actividad sin ser superior: ve todo, sin botones.

### A medias

**Bloqueo de IP en desarrollo (16-09):** iniciar sesion desde el emulador daba «Tu cuenta no tiene acceso a esta aplicacion»; era el castigo por IP de `main.ts` (6 intentos fallidos = 1 h, `Retry-After` de 57 min), no permisos ni BD. La app traduce cualquier 403 con ese texto (`AuthErrorMapper`), conviene afinarlo. Ahora la red local queda exenta del bloqueo si `NODE_ENV != production`; verificado con 8 intentos fallidos seguidos (401 en todos). En produccion sigue igual.

**Avance 16-09:** Android ya compila con el formulario Core de crear/asignar (`5509614c`, 165 pruebas) y quedó instalado en el emulador `nexara_phone` entrando directo a Actividades (sin hub de paneles; pestañas Actividades/Asistencias/Chat/Mi perfil). iOS: `2c0eecac` (asignar desde el día de una persona) y `6c234d16` (Clientes de Core, Mi perfil editable, chat por socket con menciones y `aroundId`, filtros de avisos) con `ios-static-check` limpio, sin compilar. En curso por agentes: Asistencias/Comidas/GPS de jornada en iOS (`AsistenciasRepository.swift`, `UI/Core/Asistencias/`, `ShiftGpsTracker.swift`, Info.plist) y en Android, más Clientes/perfil/chat/avisos y enlaces no-Core en Android. Nota de campo: el emulador se cayó una vez (exit 139) y perdió la instalación; y la API bloquea una IP por 6 intentos fallidos durante 1 h (`IP_BAN_MAX_STRIKES`/`IP_BAN_WINDOW_MS` en `main.ts`), se limpia reiniciando el contenedor.

**Apps solo-ERP (pedido de Adam 15-09):** sin menú de paneles ni vistas fuera de ERP; portal solo para cuentas cliente/sucursal; GPS de jornada se activa al checar entrada desde el cliente (`PATCH gps/consent`) y la API lo apaga en la salida. Rescate WIP `77b16031`: borrados CRM/STUDIO/LAB/INTEGRA/OPS/Contabilidad en Android (186 archivos) e iOS (169). **Android NO compila en `77b16031`**: `ConsoleNavHost` aún referencia `OpsNewActivityScreen` (borrado) hasta que exista el formulario Core de crear/asignar actividad. iOS sin verificar tras el borrado (`python scripts/ios-static-check.py`). En curso: formulario Core en ambas; después Asistencias (scope subtree, CEO sin checada, pestañas, Trayectoria, GPS de jornada), Clientes `erp-clients`, Mi perfil editable, Chat iOS en tiempo real, campana de avisos. Emulador: `nexara_phone`; build `./gradlew.bat :app:assembleDebug -PDEV_API_BASE_URL=http://10.0.2.2:4000/api` (Docker expone la API en el 4000).

### Siguiente

**Pendientes de Adam:** contraseñas de José Antonio, Daniela Hernández, Juan José, Roberto y Paulina; llave APNs `.p8` en Firebase; corregir en RH las nomenclaturas marcadas (punto 22), RFC/CURP/NSS faltantes de Mónica e Israel y el correo provisional de Roberto.

**Servidor de prueba** (Hetzner `5.78.215.109`, SSH 2222, ENTORNO DE PRUEBA) se despliega con cada commit de este turno; para volver a desplegar: `cd /var/www/nexara-app && ./deploy/update.sh --force-all --with-migrate` (el script hace el pull; si se hace `git pull` antes, sin `--force-all` no reconstruye). Pendiente del barrido de textos: portal de cliente/sucursal aún dice «Tickets», «Inventarios», «Cotizaciones» (Android `ui/tickets/*`, iOS `UI/Portal/*`); `AuthErrorMapper` 403.

Compilar iOS en CI y probar ambas apps en dispositivo (cámara, GPS, push, comidas). Luego Cursor ejecuta `.ai/EXEC-PACKET.md` (usabilidad Actividades/Asistencia). Al desplegar a producción subir juntas `20260913180000_drop_evidence_activity_unique_index`, `20260913190000_assignee_dispatch_log`, `20260914050000_activity_schedule_changes`, `20260914100000_evidence_photos_geo`, `20260914160000_evidence_reviews` y `20260914190000_lunch_break_review`.

### No tocar

Puente NAS.

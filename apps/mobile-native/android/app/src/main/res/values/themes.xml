<resources>
    <!-- Tema base de la aplicación. La UI real se renderiza con Jetpack Compose + MaterialTheme. -->
    <style name="Theme.Nexara" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>

    <style name="Theme.Nexara.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/nexara_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/nexara_primary</item>
        <item name="postSplashScreenTheme">@style/Theme.Nexara</item>
        <!-- Fallback for API &lt; 31 -->
        <item name="android:windowBackground">@color/nexara_primary</item>
        <item name="android:statusBarColor">@color/nexara_primary</item>
        <item name="android:navigationBarColor">@color/nexara_primary</item>
    </style>
</resources>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
      Rastreo de jornada: entre la entrada y la salida la app envía la ubicación
      aunque la pantalla esté apagada, para dibujar el trayecto del día en el
      ERP. Mientras ocurre hay una notificación permanente que lo dice, y se
      apaga sola al registrar la salida.
      Sin ACCESS_BACKGROUND_LOCATION a propósito: el servicio en primer plano de tipo
      «location» se arranca desde la checada (app abierta) y conserva el acceso a la
      ubicación con la pantalla apagada; pedir «todo el tiempo» obliga a otra
      declaración en Google Play y no aporta nada al trayecto.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".NexaraApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Nexara"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:isMonitoringTool="enterprise_management">

        <!--
          Declaración de herramienta de monitorización (política de stalkerware de
          Google Play). La app rastrea la ubicación del trabajador entre la checada
          de entrada y la de salida, así que Play la clasifica como herramienta de
          monitorización y exige declararlo aquí. Sin esta marca la revisión se
          rechaza: fue el motivo del rechazo del App Bundle 11 (22-09-2026).
        -->

        <!-- Google Maps API key. Reemplazar el placeholder con la key real. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- GPS de jornada: vive entre la entrada y la salida, en primer plano. -->
        <service
            android:name=".util.JornadaGpsService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="location" />

        <!-- Firebase Cloud Messaging -->
        <service
            android:name=".push.NexaraFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="nx2_default" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_nexara" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/nexara_primary" />
        <!-- Crashlytics sí; Analytics/AD_ID no (Play Console). -->
        <meta-data
            android:name="firebase_analytics_collection_deactivated"
            android:value="true" />
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/nexara_file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Nexara.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nexara" />
            </intent-filter>
        </activity>

    </application>

</manifest>


/**
 * Seed principal — Core ola1 roster (CEO/David + managers blank).
 * Orquestador: seed-core-roster.ts
 *
 * Run:
 *   npm run prisma:seed
 *   or
 *   npx ts-node prisma/seed.ts
 */

import('./seed-core-roster');

console.log('✨ Seed orquestador → seed-core-roster.');

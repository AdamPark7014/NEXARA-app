/**
 * Seed principal — SOLO usuarios del organigrama NEXARA.
 * Orquestador simple que ejecuta seed-demo-users.ts
 *
 * Run:
 *   npm run prisma:seed
 *   or
 *   npx ts-node prisma/seed.ts
 */

import('./seed-demo-users');

console.log('✨ Seed completado.');
